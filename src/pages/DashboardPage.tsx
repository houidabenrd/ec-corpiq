import { LayoutDashboard } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export function DashboardPage() {
  return <PlaceholderPage title="Tableau de bord" icon={<LayoutDashboard size={28} />} />;
}
